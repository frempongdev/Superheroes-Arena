import '../style/Hero.css';
import HeroCarousel from './HeroCarousel';

const Hero = () => (
  <div className="hero">
    <div className="down-box">
      <HeroCarousel />
    </div>
  </div>
);

export default Hero;
